{
  "framework": "nextjs",
  "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" == \"main\" ]]; then exit 1; fi; if [[ \"$VERCEL_GIT_COMMIT_REF\" == *github-actions* ]]; then exit 0; fi; if [[ -n \"$VERCEL_GIT_PULL_REQUEST_NUMBER\" ]]; then exit 1; fi; exit 0"
}
