name: Trivy Security Scan

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  trivy:
    uses: krypsis-io/.github/.github/workflows/trivy.yml@main
